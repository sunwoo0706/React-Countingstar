import React from 'react';

export interface NumberListWrapperProps {
    idx: number;
    time: number;
    length: number;
}

export interface NumberListProps {
    start: number;
    idx: number;
    time: number;
    length: number;
}